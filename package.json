{
  "name": "trinity-process-solutions",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,yellow \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
